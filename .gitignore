
# Build output
# Combined SQL scratch
# Dependencies
# Editors
# Environment / secrets — never commit
# Local secrets reference files — NEVER commit these
# Logs
# OS
# Screenshots local
# Tauri runtime config (dev only)
**/node_modules/
*.log
*.secrets
*.swp
.DS_Store
.env
.env.*.local
.env.local
.idea/
.vscode/
SECRETS.local
SECRETS.local.md
Screenshot*.png
Thumbs.db
agent.pem
agent/dist-mac/
agent/dist/
agent/src-tauri/.cargo/
agent/src-tauri/WixTools/
agent/src-tauri/gen/schemas/
agent/src-tauri/target/
dist-mac/
dist/
node_modules/
npm-debug.log*
out/
supabase/_combined_setup.sql
.vercel

# Self-signed code-signing material — NEVER commit private keys.
# Public cert (RudransRootCA.cer) is committed under agent/src-tauri/resources/
# so the .pkg postinstall can import it. Everything else stays local.
.signing/
*.p12
*.p12.b64
RudransRootCA.key
leaf.key
leaf.csr
leaf.ext
*.srl
